# SMUS Admin Agent Configuration

# Anthropic API Configuration (REQUIRED)
ANTHROPIC_API_KEY=your_anthropic_api_key_here
DEFAULT_MODEL=claude-3-5-sonnet-20241022
MAX_TOKENS=4096
TEMPERATURE=0.7

# MCP Server Configuration (OPTIONAL)
# Option 1: Single MCP Server
MCP_SERVER_PATH=/path/to/your/mcp-server

# Option 2: Multiple MCP Servers (JSON format)
# Uncomment and modify the line below to use multiple servers
# MCP_SERVERS_CONFIG='[{"name": "datazone", "path": "/path/to/amazon-datazone-mcp-server", "module": "datazone_mcp_server.server"}, {"name": "other", "path": "/path/to/other-server", "module": "other_server.server"}]'

# AWS Configuration (for DataZone MCP server)
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_DEFAULT_REGION=us-east-1